{
	"name": "redis-provider",
	"version": "1.0.0",
	"description": "",
	"main": "RedisModule.js",
	"scripts": {
		"build": "nest build",
		"format": "prettier --write \"packages/**/*.ts\" \"tests/**/*.ts\"",
		"lint": "eslint -c .eslintrc.json \"{src,apps,libs,tests,scripts}/**/*.ts\" --fix",
		"bump": "lerna version patch --skip-git --yes"
	},
	"dependencies": {
		"redis": "^4.7.0"
	},
	"devDependencies": {
		"@types/redis": "^4.0.11"
	}
}
