{
	"name": "config-provider",
	"version": "1.0.9",
	"description": "The config-provider package offers configuration management functionalities for applications built with the NestJS framework. It includes various providers and interfaces to manage configuration settings, such as IConfigProvider. The package ensures type safety with TypeScript.",
	"main": "ConfigProviderModule.js",
	"scripts": {
		"build": "nest build",
		"test": "jest"
	},
	"dependencies": {
		"nconf": "^0.12.1"
	},
	"devDependencies": {
		"@types/nconf": "^0.10.6"
	},
	"publishConfig": {
		"@databox:registry": "https://npm.pkg.github.com"
	}
}
